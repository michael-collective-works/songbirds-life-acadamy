// app/layout.tsx
import type { Metadata } from 'next'
import { SiteHeader } from '@/components/site-header'
import { ShapeBackground } from '@/components/shape-background'
import './globals.css'

export const metadata: Metadata = {
  metadataBase: new URL('https://songbirdslifeacademy.com'),
  title: {
    default: 'Songbirds Life Academy (S.L.A.Y.)',
    template: '%s · S.L.A.Y.',
  },
  description:
    'Songbirds Life Academy (S.L.A.Y.) offers infant-toddler care, preschool, and after-school programs in a warm, music-infused environment in St. Francisville, LA.',
  keywords: [
    'daycare',
    'infant care',
    'toddler care',
    'preschool',
    'after school',
    'early childhood education',
    'St. Francisville',
    'Louisiana',
    'music education',
    'S.L.A.Y.',
    'Songbirds Life Academy',
  ],
  openGraph: {
    title: 'Songbirds Life Academy (S.L.A.Y.)',
    description:
      'A joyful start for every child—learning through play, immersed in music. Infant–Toddler, Preschool, and After-School programs.',
    url: 'https://songbirdslifeacademy.com',
    siteName: 'Songbirds Life Academy',
    type: 'website',
    locale: 'en_US',
    images: [
      {
        url: '/images/logo-ios-full.jpg',
        width: 1080,
        height: 1920,
        alt: 'S.L.A.Y. artwork and logotype on cream background',
      },
    ],
  },
  twitter: {
    card: 'summary_large_image',
    title: 'Songbirds Life Academy (S.L.A.Y.)',
    description:
      'A joyful start for every child—learning through play, immersed in music. Infant–Toddler, Preschool, and After-School programs.',
    images: ['/images/logo-ios-full.jpg'],
  },
  alternates: {
    canonical: 'https://songbirdslifeacademy.com',
  },
  themeColor: [
    { media: '(prefers-color-scheme: light)', color: '#0f766e' },
    { media: '(prefers-color-scheme: dark)', color: '#0f766e' },
  ],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className="relative">
        <ShapeBackground />
        <SiteHeader />
        <div id="main" />
        {children}
      </body>
    </html>
  )
}

// components/site-header.tsx
import React from 'react'
import { cn } from '@/lib/utils'

interface SiteHeaderProps extends React.HTMLAttributes<HTMLElement> {}

export default function SiteHeader({ className, ...props }: SiteHeaderProps) {
  return (
    <>
      <a
        href="#main"
        className="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-2 focus:z-50 focus:rounded focus:bg-white focus:px-3 focus:py-2 focus:text-slate-900 focus:shadow"
      >
        Skip to content
      </a>
      <header className={cn('w-full border-b bg-white/80 backdrop-blur supports-[backdrop-filter]:bg-white/60', className)}>
        {/* existing header content here */}
      </header>
    </>
  )
}

// components/shape-background.tsx
import React from 'react'

export default function ShapeBackground() {
  return (
    <div className="relative">
      <div className="absolute -left-24 -top-28 h-72 w-72 rounded-[40%_60%_65%_35%/45%_30%_70%_55%] bg-teal-300/25 blur-2xl" />
      <div className="absolute -right-14 top-8 h-48 w-48 rounded-[61%_39%_73%_27%/45%_67%_33%_55%] bg-amber-300/25 blur-2xl" />
      <div className="absolute -bottom-12 left-14 h-64 w-64 rounded-[39%_61%_38%_62%/58%_29%_71%_42%] bg-sky-300/25 blur-2xl" />
    </div>
  )
}

// app/about/page.tsx
import Image from 'next/image'
import Link from 'next/link'
import { SiteHeader } from '@/components/site-header'
import { SiteFooter } from '@/components/site-footer'
import { listTeam } from '@/lib/db'
import { Button } from '@/components/ui/button'

export default async function AboutPage() {
  const team = await listTeam()

  return (
    <main id="main" className="flex min-h-screen flex-col bg-white">
      <SiteHeader />
      <section className="mx-auto max-w-5xl px-4 py-12">
        <header className="grid items-start gap-8 md:grid-cols-[280px,1fr]">
          <div className="flex flex-col items-center">
            <Image
              src="/images/logo-original.svg"
              alt="Songbirds Life Academy logo"
              width={180}
              height={180}
              className="h-28 w-28"
            />
            <p className="mt-3 text-center text-sm text-slate-600">{'Learning through play, immersed in music.'}</p>
          </div>
          <div className="prose prose-slate max-w-none">
            <h1>About Songbirds Life Academy</h1>
            <p className="lead">
              Songbirds Life Academy serves families with a warm, creative approach grounded in research-based practice.
              Music sits at the heart of daily routines—supporting bonding, language, social growth, and motor skills.
            </p>
            <p>
              We implement The Creative Curriculum® across infant, toddler, and preschool classrooms, transforming everyday
              moments into meaningful learning. Our team fosters social-emotional, physical, cognitive, and language development
              through responsive, play-centered experiences.
            </p>
            <div className="not-prose mt-6">
              <Button asChild className="bg-teal-700 hover:bg-teal-800">
                <Link href="/join">Join our team</Link>
              </Button>
            </div>
          </div>
        </header>

        <div className="mt-12">
          <h2 className="mb-4 text-2xl font-semibold text-slate-900">Our Team</h2>
          {team?.length ? (
            <ul className="grid gap-6 sm:grid-cols-2">
              {team.map((m: any) => (
                <li key={m.id} className="rounded-xl border p-5 shadow-sm">
                  <div className="flex items-start gap-5">
                    <div className="h-24 w-24 flex-shrink-0 overflow-hidden rounded-full bg-amber-100 ring-1 ring-amber-200">
                      {m.headshot_url ? (
                        <Image
                          src={m.headshot_url || "/placeholder.svg"}
                          alt={`${m.name} headshot`}
                          width={96}
                          height={96}
                          className="h-24 w-24 object-cover"
                        />
                      ) : (
                        <Image
                          src="/images/logo-original.svg"
                          alt=""
                          width={96}
                          height={96}
                          className="h-24 w-24 p-3 opacity-70"
                        />
                      )}
                    </div>
                    <div>
                      <div className="text-xl font-semibold text-slate-900">{m.name}</div>
                      <div className="text-sm text-slate-600">{m.title}</div>
                      <p className="mt-3 text-[15px] leading-6 text-slate-700">{m.bio}</p>
                    </div>
                  </div>
                </li>
              ))}
            </ul>
          ) : (
            <p className="text-slate-600">Team profiles coming soon.</p>
          )}
        </div>
      </section>
      <SiteFooter />
    </main>
  )
}

// app/events/page.tsx
import { SiteHeader } from '@/components/site-header'
import { SiteFooter } from '@/components/site-footer'
import { listEvents } from '@/lib/db'

export default async function EventsPage() {
  const events = await listEvents()

  return (
    <main id="main" className="flex min-h-screen flex-col bg-white">
      <SiteHeader />
      <section className="mx-auto max-w-3xl px-4 py-12">
        <h1 className="mb-8 text-3xl font-semibold text-slate-900">Events</h1>

        {events?.length ? (
          <ol className="relative border-l border-slate-200">
            {events.map((ev: any) => (
              <li key={ev.id} className="mb-8 ml-6">
                <span className="absolute -left-[9px] mt-1 h-4 w-4 rounded-full border border-teal-700 bg-teal-50" />
                <h3 className="text-lg font-semibold text-slate-900">{ev.title}</h3>
                <p className="text-sm text-slate-600">
                  {ev.date}{ev.time ? ` • ${ev.time}` : ''}
                </p>
                <p className="mt-2 text-slate-700">{ev.description}</p>
              </li>
            ))}
          </ol>
        ) : (
          <p className="text-slate-600">No events yet. Check back soon!</p>
        )}
      </section>
      <SiteFooter />
    </main>
  )
}

// app/store/page.tsx
import Image from 'next/image'
import Link from 'next/link'
import { SiteHeader } from '@/components/site-header'
import { SiteFooter } from '@/components/site-footer'
import { listStore } from '@/lib/db'
import { ShoppingBag } from 'lucide-react'

const SQUARE_URL = 'http://feathered-insights-by-songbirds.square.site/'

export default async function StorePage() {
  const items = await listStore()
  const uniforms = items.filter((i: any) => i.category === 'uniform')
  const merch = items.filter((i: any) => i.category === 'merch')

  return (
    <main id="main" className="flex min-h-screen flex-col bg-white">
      <SiteHeader />
      <section className="mx-auto max-w-6xl px-4 py-12">
        <div className="mb-8">
          <h1 className="text-3xl font-semibold text-slate-900">Store</h1>
          <p className="mt-2 max-w-prose text-slate-700">
            Our uniforms and merch reflect the Songbirds spirit—warm, creative, and rooted in music. From classroom-ready
            essentials to weekend favorites, gear up with items that help children feel part of a joyful learning community.
          </p>
          <ul className="mt-3 list-inside list-disc text-sm text-slate-700">
            <li>Uniforms designed for comfort, movement, and play</li>
            <li>Merch that celebrates creativity and our musical identity</li>
            <li>Secure checkout handled by our Square storefront</li>
          </ul>
          <Link
            href={SQUARE_URL}
            target="_blank"
            rel="noopener noreferrer"
            className="mt-4 inline-flex items-center gap-2 rounded-md bg-amber-100 px-3 py-2 text-sm font-medium text-amber-900 ring-1 ring-amber-200 hover:bg-amber-200"
          >
            <ShoppingBag className="h-4 w-4" />
            Shop on Square (opens new tab)
          </Link>
        </div>

        <div className="space-y-10">
          <div>
            <h2 className="mb-4 text-xl font-semibold text-slate-900">Uniforms</h2>
            {uniforms.length ? (
              <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
                {uniforms.map((item: any) => (
                  <Link key={item.id} href={item.external_url} target="_blank" rel="noopener noreferrer" className="group">
                    <div className="overflow-hidden rounded-lg border bg-white shadow-sm transition hover:shadow-md">
                      <div className="relative aspect-[4/3]">
                        <Image src={item.image_url || '/images/logo-original.svg'} alt={item.title} fill className="object-cover p-4" />
                      </div>
                      <div className="p-3">
                        <p className="text-sm font-medium text-slate-900 group-hover:text-teal-800">{item.title}</p>
                        <p className="text-xs text-slate-600">View on Square</p>
                      </div>
                    </Link>
                  ))}
                </div>
              ) : (
                <p className="text-sm text-slate-600">Coming soon.</p>
              )}
            </div>

            <div>
              <h2 className="mb-4 text-xl font-semibold text-slate-900">Merch</h2>
              {merch.length ? (
                <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
                  {merch.map((item: any) => (
                    <Link key={item.id} href={item.external_url} target="_blank" rel="noopener noreferrer" className="group">
                      <div className="overflow-hidden rounded-lg border bg-white shadow-sm transition hover:shadow-md">
                        <div className="relative aspect-[4/3]">
                          <Image src={item.image_url || '/images/logo-original.svg'} alt={item.title} fill className="object-cover p-4" />
                        </div>
                        <div className="p-3">
                          <p className="text-sm font-medium text-slate-900 group-hover:text-teal-800">{item.title}</p>
                          <p className="text-xs text-slate-600">View on Square</p>
                        </div>
                      </div>
                    </Link>
                  ))}
                </div>
              ) : (
                <p className="text-sm text-slate-600">Coming soon.</p>
              )}
            </div>
          </div>
        </section>
        <SiteFooter />
      </main>
    )
  }

// app/join/page.tsx
import { SiteHeader } from '@/components/site-header'
import { SiteFooter } from '@/components/site-footer'
import { JoinForm } from '@/components/join-form'
import { Music, Sparkles, Heart } from 'lucide-react'

export default function JoinPage() {
  return (
    <main id="main" className="flex min-h-screen flex-col bg-white">
      <SiteHeader />
      <section className="mx-auto max-w-5xl px-4 py-12">
        <div className="grid gap-6 lg:grid-cols-2">
          <div className="rounded-xl border bg-gradient-to-br from-amber-50 to-teal-50 p-6">
            <h1 className="text-3xl font-semibold text-slate-900">Join the Songbirds Team</h1>
            <p className="mt-2 text-slate-700">
              Help us build a warm, creative community where children learn through play and music.
            </p>
            <ul className="mt-4 space-y-2 text-sm text-slate-700">
              <li className="flex items-start gap-2"><Music className="mt-0.5 h-4 w-4 text-teal-700" /> Music-infused learning environment</li>
              <li className="flex items-start gap-2"><Sparkles className="mt-0.5 h-4 w-4 text-teal-700" /> Creative curriculum and low ratios</li>
              <li className="flex items-start gap-2"><Heart className="mt-0.5 h-4 w-4 text-teal-700" /> Caring, family-centered culture</li>
            </ul>
            <p className="mt-3 text-xs text-slate-600">
              Hours: Mon–Fri 6:30am–5:30pm · Extended hours available upon request
            </p>
          </div>
          <div className="rounded-xl border bg-white p-6 shadow-sm">
            <h2 className="mb-4 text-xl font-semibold text-slate-900">Send your info</h2>
            <JoinForm />
          </div>
        </div>
      </section>
      <SiteFooter />
    </main>
  )
}

// components/site-footer.tsx
import Link from 'next/link'
import { Instagram, Facebook, MapPin, Phone, Clock } from 'lucide-react'

export function SiteFooter() {
  return (
    <footer className="mt-16 border-t bg-amber-50/50">
      <div className="mx-auto grid max-w-6xl gap-8 px-4 py-10 sm:grid-cols-2 lg:grid-cols-4">
        <div>
          <h3 className="font-semibold text-slate-900">S.L.A.Y.</h3>
          <p className="mt-2 text-sm text-slate-700">{'Learning through play, immersed in music.'}</p>
          <p className="mt-4 text-xs text-slate-500">
            Songbirds Life Academy for the Youth
          </p>
        </div>

        <div>
          <h4 className="font-semibold text-slate-900">Visit</h4>
          <ul className="mt-2 space-y-2 text-sm text-slate-700">
            <li className="flex items-start gap-2">
              <MapPin className="mt-0.5 h-4 w-4" />
              <a
                className="hover:underline"
                href="https://maps.google.com/?q=5915%20N.%20Commerce%20Street,%20St.Francisville,%20LA%2070775"
                target="_blank"
                rel="noopener noreferrer"
              >
                5915 N. Commerce Street, St.Francisville, LA 70775
              </a>
            </li>
            <li className="flex items-center gap-2">
              <Clock className="h-4 w-4" />
              <span>Mon–Fri 6:30am–5:30pm</span>
            </li>
            <li className="ml-6 text-xs text-slate-600">{'*Extended hours available upon request'}</li>
          </ul>
        </div>

        <div>
          <h4 className="font-semibold text-slate-900">Contact</h4>
          <ul className="mt-2 space-y-2 text-sm text-slate-700">
            <li className="flex items-center gap-2">
              <Phone className="h-4 w-4" />
              <a className="hover:underline" href="tel:12256357973">
                225.635.7973
              </a>
            </li>
            <li>
              <Link className="text-teal-700 hover:underline" href="/join">
                Join our team or volunteer
              </Link>
            </li>
          </ul>
        </div>

        <div>
          <h4 className="font-semibold text-slate-900">Social</h4>
          <ul className="mt-2 space-y-2 text-sm text-slate-700">
            <li className="flex items-center gap-2">
              <Instagram className="h-4 w-4" />
              <a
                className="hover:underline"
                href="https://instagram.com/songbirdslifeacademy"
                target="_blank"
                rel="noopener noreferrer"
              >
                @songbirdslifeacademy
              </a>
            </li>
            <li className="flex items-center gap-2">
              <Facebook className="h-4 w-4" />
              <a
                className="hover:underline"
                href="https://facebook.com/songbirdslifeacademy"
                target="_blank"
                rel="noopener noreferrer"
              >
                @songbirdslifeacademy
              </a>
            </li>
          </ul>
        </div>
      </div>

      <div className="border-t py-4">
        <div className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-3 px-4 text-xs text-slate-500 sm:flex-row">
          <p>{'© '}{new Date().getFullYear()}{' Songbirds Life Academy. All rights reserved.'}</p>
          <nav className="flex items-center gap-4">
            <Link className="hover:underline" href="/about">About</Link>
            <Link className="hover:underline" href="/events">Events</Link>
            <Link className="hover:underline" href="/store">Store</Link>
            <Link className="hover:underline" href="/join">Join Us</Link>
            <Link className="hover:underline text-slate-400" href="/admin" aria-label="Admin sign in">
              Admin
            </Link>
          </nav>
        </div>
      </div>
    </footer>
  )
}

// components/join-form.tsx
'use client'

import { useState } from 'react'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Textarea } from '@/components/ui/textarea'
import { Button } from '@/components/ui/button'
import { RadioGroup, RadioGroupItem } from '@/components/ui/radio-group'
import { CheckCircle2 } from 'lucide-react'

export function JoinForm() {
  const [status, setStatus] = useState<'idle' | 'submitting' | 'success' | 'error'>('idle')
  const [error, setError] = useState<string | null>(null)

  async function onSubmit(form: FormData) {
    setStatus('submitting')
    setError(null)
    const payload = Object.fromEntries(form.entries())
    try {
      const res = await fetch('/api/join-us', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload),
      })
      if (!res.ok) throw new Error(await res.text())
      setStatus('success')
    } catch {
      setError('Something went wrong. Please try again.')
      setStatus('error')
    }
  }

  if (status === 'success') {
    return (
      <div className="rounded-lg border border-teal-200 bg-teal-50 p-6 text-teal-900">
        <div className="flex items-start gap-3">
          <CheckCircle2 className="mt-0.5 h-5 w-5" />
          <div>
            <h3 className="font-semibold">Thanks for reaching out!</h3>
            <p className="text-sm">We received your submission and will contact you soon.</p>
          </div>
        </div>
      </div>
    )
  }

  return (
    <form className="grid gap-6" action={(fd: FormData) => onSubmit(fd)}>
      {/* Honeypot */}
      <input type="text" name="company" tabIndex={-1} autoComplete="off" className="hidden" aria-hidden="true" />

      <div className="grid gap-4 sm:grid-cols-2">
        <div className="space-y-1.5">
          <Label htmlFor="first_name">First name</Label>
          <Input id="first_name" name="first_name" required />
        </div>
        <div className="space-y-1.5">
          <Label htmlFor="last_name">Last name</Label>
          <Input id="last_name" name="last_name" required />
        </div>
      </div>

      <div className="grid gap-4 sm:grid-cols-2">
        <div className="space-y-1.5">
          <Label htmlFor="email">Email</Label>
          <Input id="email" name="email" type="email" required />
        </div>
        <div className="space-y-1.5">
          <Label htmlFor="phone">Phone</Label>
          <Input id="phone" name="phone" inputMode="tel" required />
        </div>
      </div>

      <div className="space-y-1.5">
        <Label>I’m interested in</Label>
        <RadioGroup name="kind" defaultValue="employment" className="grid grid-cols-2 gap-2 sm:max-w-sm">
          <div className="flex items-center gap-2 rounded-md border p-2">
            <RadioGroupItem id="employment" value="employment" />
            <Label htmlFor="employment" className="cursor-pointer">Employment</Label>
          </div>
          <div className="flex items-center gap-2 rounded-md border p-2">
            <RadioGroupItem id="volunteer" value="volunteer" />
            <Label htmlFor="volunteer" className="cursor-pointer">Volunteer</Label>
          </div>
        </RadioGroup>
      </div>

      <div className="space-y-1.5">
        <Label htmlFor="message">Message (optional)</Label>
        <Textarea id="message" name="message" rows={3} placeholder="Tell us a little about yourself" />
      </div>

      <div className="flex items-center gap-3">
        <Button type="submit" disabled={status === 'submitting'}>
          {status === 'submitting' ? 'Submitting…' : 'Submit'}
        </Button>
        {error && <p className="text-sm text-red-600">{error}</p>}
      </div>

      <p className="text-xs text-slate-500">
        We’ll never share your contact information. Protected by rate limiting and spam checks.
      </p>
    </form>
  }

// components/admin/admin-nav.tsx
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { cn } from '@/lib/utils'

const links = [
  { href: '/admin', label: 'Dashboard' },
  { href: '/admin/events', label: 'Events' },
  { href: '/admin/team', label: 'Team' },
  { href: '/admin/store', label: 'Store' },
  { href: '/admin/applications', label: 'Applications' },
]

export function AdminNav() {
  const pathname = usePathname()

  return (
    <nav className="flex gap-2" role="navigation" aria-label="Admin navigation">
      {links.map((l) => (
        <Link
          key={l.href}
          href={l.href}
          aria-current={pathname === l.href ? 'page' : undefined}
          className={cn(
            'rounded-md px-3 py-2 text-sm',
            pathname === l.href ? 'bg-teal-700 text-white' : 'text-slate-700 hover:bg-slate-100'
          )}
        >
          {l.label}
        </Link>
      ))}
    </nav>
  )
}

// app/admin/page.tsx
import Link from 'next/link'

export const dynamic = 'force-dynamic'

const cards = [
  { title: 'Events', desc: 'Create and manage events', href: '/admin/events' },
  { title: 'Team', desc: 'Manage team members', href: '/admin/team' },
  { title: 'Store', desc: 'Manage store items', href: '/admin/store' },
  { title: 'Applications', desc: 'Review and archive Join Us submissions', href: '/admin/applications' },
]

export default function AdminPage() {
  return (
    <div className="grid gap-4 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
      {cards.map((card) => (
        <Link key={card.title} href={card.href} className="group rounded-md border p-4 hover:bg-slate-50">
          <h3 className="text-lg font-medium text-slate-900 group-hover:text-teal-800">{card.title}</h3>
          <p className="text-sm text-slate-600">{card.desc}</p>
        </Link>
      ))}
    </div>
  )
}

// lib/db.ts
import { unstable_noStore as noStore } from 'next/cache'
import { sql } from '@vercel/postgres'
import { Event, Team, Store, Application } from '@/lib/definitions'
import { memory } from '@/lib/memory'

const useDb = process.env.USE_DB === 'true'

export async function listEvents() {
  noStore() // bust cache
  if (!useDb) return memory.events
  const rows = await (sql as any)`
    select id, title, description, date, time
    from events
    order by date
  `
  return rows as Event[]
}

export async function getEvent(id: string) {
  noStore()
  if (!useDb) return memory.events.find((e) => e.id === id) ?? null
  const rows = await (sql as any)`
    select id, title, description, date, time
    from events
    where id=${id}
  `
  return rows[0] ?? null
}

export async function listTeam() {
  noStore()
  if (!useDb) return memory.team
  const rows = await (sql as any)`
    select id, name, title, bio, headshot_url
    from team
    order by name
  `
  return rows as Team[]
}

export async function getTeam(id: string) {
  noStore()
  if (!useDb) return memory.team.find((t) => t.id === id) ?? null
  const rows = await (sql as any)`
    select id, name, title, bio, headshot_url
    from team
    where id=${id}
  `
  return rows[0] ?? null
}

export async function listStore() {
  noStore()
  if (!useDb) return memory.store
  const rows = await (sql as any)`
    select id, title, description, category, image_url, external_url
    from store
    order by title
  `
  return rows as Store[]
}

export async function getStore(id: string) {
  noStore()
  if (!useDb) return memory.store.find((s) => s.id === id) ?? null
  const rows = await (sql as any)`
    select id, title, description, category, image_url, external_url
    from store
    where id=${id}
  `
  return rows[0] ?? null
}

export async function listApplications() {
  // newest first for convenience
  if (!useDb) return memory.applications.slice().reverse()
  const rows = await (sql as any)`
    select id, first_name, last_name, email, phone, kind, status
    from applications
    order by created_at desc
  `
  return rows
}

export async function getApplication(id: string) {
  if (!useDb) return memory.applications.find((a) => a.id === id) ?? null
  const rows = await (sql as any)`
    select id, first_name, last_name, email, phone, kind, status
    from applications
    where id=${id}
  `
  return rows[0] ?? null
}

export async function updateApplicationStatus(id: string, status: 'new' | 'reviewed' | 'archived') {
  if (!useDb) {
    const i = memory.applications.findIndex((a) => a.id === id)
    if (i === -1) return null
    memory.applications[i].status = status
    return memory.applications[i]
  }
  const rows = await (sql as any)`
    update applications
    set status=${status}
    where id=${id}
    returning id, first_name, last_name, email, phone, kind, status
  `
  return rows[0] ?? null
}

// app/admin/layout.tsx
import { AdminNav } from '@/components/admin/admin-nav'

export const dynamic = 'force-dynamic'

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex min-h-screen flex-col">
      <a
        href="#main"
        className="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-2 focus:z-50 focus:rounded focus:bg-white focus:px-3 focus:py-2 focus:text-slate-900 focus:shadow"
      >
        Skip to content
      </a>
      <main id="main" className="mx-auto max-w-6xl px-4 py-6">
        <AdminNav />
        <div className="mt-8">{children}</div>
      </main>
    </div>
  )
}

// app/admin/events/page.tsx
export const dynamic = 'force-dynamic'

import { requireAdmin } from '@/lib/authz'
import { redirect } from 'next/navigation'
import EventsManager from '@/components/admin/events-manager'

export default async function AdminEventsPage() {
  const authResult = await requireAdmin()
  if (!authResult.ok) return redirect('/admin/sign-in')

  return (
    <div>
      <EventsManager />
    </div>
  )
}

// app/admin/team/page.tsx
export const dynamic = 'force-dynamic'

import { requireAdmin } from '@/lib/authz'
import { redirect } from 'next/navigation'
import TeamManager from '@/components/admin/team-manager'

export default async function AdminTeamPage() {
  const authResult = await requireAdmin()
  if (!authResult.ok) return redirect('/admin/sign-in')

  return (
    <div>
      <TeamManager />
    </div>
  )
}

// app/admin/store/page.tsx
export const dynamic = 'force-dynamic'

import { requireAdmin } from '@/lib/authz'
import { redirect } from 'next/navigation'
import StoreManager from '@/components/admin/store-manager'

export default async function AdminStorePage() {
  const authResult = await requireAdmin()
  if (!authResult.ok) return redirect('/admin/sign-in')

  return (
    <div>
      <StoreManager />
    </div>
  )
}

// app/admin/applications/page.tsx
export const dynamic = 'force-dynamic'

import { requireAdmin } from '@/lib/authz'
import { redirect } from 'next/navigation'
import ApplicationsManager from '@/components/admin/applications-manager'

export default async function AdminApplicationsPage() {
  const authResult = await requireAdmin()
  if (!authResult.ok) return redirect('/admin/sign-in')

  return (
    <div>
      <ApplicationsManager />
    </div>
  )
}

// lib/authz.ts
import type { Session } from 'next-auth'

export async function requireAdmin() {
  try {
    const hasAuthEnv =
      Boolean(process.env.GOOGLE_CLIENT_ID) &&
      Boolean(process.env.GOOGLE_CLIENT_SECRET) &&
      Boolean(process.env.NEXTAUTH_SECRET)

    // If auth isn't configured, treat as unauthorized (preview-safe)
    if (!hasAuthEnv) return { ok: false as const, status: 401 as const }

    // Lazy import to avoid module init errors when env is missing
    const mod = await import('@/auth')
    const auth = mod?.auth as (() => Promise<Session | null>) | undefined
    if (!auth) return { ok: false as const, status: 401 as const }

    const session = await auth()
    const role = (session?.user as any)?.role as string | undefined
    if (!session || !role || !['owner', 'editor'].includes(role)) {
      return { ok: false as const, status: 401 as const }
    }
    return { ok: true as const }
  } catch {
    // Never throw to the route handler; return an auth failure instead
    return { ok: false as const, status: 401 as const }
  }
}

// components/admin/applications-manager.tsx
'use client'

import { useState, useEffect } from 'react'
import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import {
  Dialog,
  DialogHeader,
  DialogTitle,
  DialogDescription,
  DialogContent,
  DialogFooter,
  DialogTrigger,
} from '@/components/ui/dialog'
import { Label } from '@/components/ui/label'
import { Textarea } from '@/components/ui/textarea'
import { CheckCircle, XCircle, Archive } from 'lucide-react'

interface Application {
  id: string
  first_name: string
  last_name: string
  email: string
  phone: string
  kind: string
  status: 'new' | 'reviewed' | 'archived'
}

export default function ApplicationsManager() {
  const [items, setItems] = useState<Application[]>([])
  const [error, setError] = useState<string | null>(null)
  const [selectedId, setSelectedId] = useState<string | null>(null)
  const [isDialogOpen, setIsDialogOpen] = useState(false)

  useEffect(() => {
    refresh()
  }, [])

  async function refresh() {
    setError(null)
    try {
      const res = await fetch('/api/admin/applications', { cache: 'no-store' })
      // Try JSON first; if it fails, fall back to text
      let data: any = null
      try {
        data = await res.json()
      } catch {
        const text = await res.text().catch(() => '')
        if (!res.ok) {
          setItems([])
          setError(text || `Request failed (${res.status})`)
          return
        }
        // If OK but not JSON (unexpected), default to empty
        data = { items: [] }
      }
      if (!res.ok) {
        const msg =
          data?.error ||
          (res.status === 401
            ? 'Please sign in as an admin to view applications.'
            : `Request failed (${res.status})`)
        setItems([])
        setError(msg)
        return
      }
      setItems(Array.isArray(data.items) ? data.items : [])
    } catch (e: any) {
      setItems([])
      setError(e?.message || 'Unable to load applications.')
    }
  }

  async function updateStatus(id: string, status: 'new' | 'reviewed' | 'archived') {
    const res = await fetch(`/api/admin/applications/${id}`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ status }),
    })
    if (!res.ok) {
      const data = await res.json()
      alert(data?.error || 'Could not update status')
      return
    }
    refresh()
    setIsDialogOpen(false)
  }

  return (
    <div>
      <header className="mb-4 flex items-center justify-between">
        <h1 className="text-2xl font-semibold text-slate-900">Applications</h1>
        <Button onClick={refresh}>Refresh</Button>
      </header>

      {error && (
        <Alert variant="destructive">
          <AlertTitle>Cannot load applications</AlertTitle>
          <AlertDescription>{error}</AlertDescription>
        </Alert>
      )}

      <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {items.map((item) => (
          <Card key={item.id}>
            <CardHeader>
              <CardTitle>
                {item.first_name} {item.last_name}
              </CardTitle>
              <CardDescription>{item.kind}</CardDescription>
            </CardHeader>
            <CardContent className="grid gap-2">
              <div className="text-sm text-slate-700">
                <Label>Email</Label>
                <p>{item.email}</p>
              </div>
              <div className="text-sm text-slate-700">
                <Label>Phone</Label>
                <p>{item.phone}</p>
              </div>
            </CardContent>
            <CardFooter className="flex items-center justify-between">
              <Badge
                variant={
                  item.status === 'new' ? 'secondary' : item.status === 'reviewed' ? 'outline' : 'destructive'
                }
              >
                {item.status}
              </Badge>
              <Dialog>
                <DialogTrigger asChild>
                  <Button onClick={() => setSelectedId(item.id)}>Manage</Button>
                </DialogTrigger>
                <DialogContent>
                  <DialogHeader>
                    <DialogTitle>Manage Application</DialogTitle>
                    <DialogDescription>
                      Take action on this application. You can mark it as reviewed, archive it, or leave it as new.
                    </DialogDescription>
                  </DialogHeader>
                  <div className="grid gap-4 py-4">
                    <div className="grid grid-cols-4 items-center gap-4">
                      <Label htmlFor="status" className="text-right">
                        Status
                      </Label>
                      <Textarea id="status" className="col-span-3" disabled value={item.status} />
                    </div>
                  </div>
                  <DialogFooter>
                    {item.status !== 'reviewed' && (
                      <Button type="submit" onClick={() => updateStatus(item.id, 'reviewed')}>
                        Mark Reviewed <CheckCircle className="ml-2 h-4 w-4" />
                      </Button>
                    )}
                    {item.status !== 'archived' && (
                      <Button variant="destructive" onClick={() => updateStatus(item.id, 'archived')}>
                        Archive <Archive className="ml-2 h-4 w-4" />
                      </Button>
                    )}
                  </DialogFooter>
                </DialogContent>
              </Dialog>
            </CardFooter>
          </Card>
        ))}
      </div>
    </div>
  )
}

// components/admin/events-manager.tsx
'use client'

import { useState, useEffect } from 'react'
import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Textarea } from '@/components/ui/textarea'

interface Event {
  id: string
  title: string
  description: string
  date: string
  time?: string
}

export default function EventsManager() {
  const [items, setItems] = useState<Event[]>([])
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    refresh()
  }, [])

  async function refresh() {
    setError(null)
    try {
      const res = await fetch('/api/admin/events', { cache: 'no-store' })
      let data: any = null
      try {
        data = await res.json()
      } catch {
        const text = await res.text().catch(() => '')
        if (!res.ok) {
          setItems([])
          setError(text || `Request failed (${res.status})`)
          return
        }
        data = { items: [] }
      }
      if (!res.ok) {
        const msg =
          data?.error ||
          (res.status === 401 ? 'Please sign in as an admin to manage events.' : `Request failed (${res.status})`)
        setItems([])
        setError(msg)
        return
      }
      setItems(Array.isArray(data.items) ? data.items : [])
    } catch (e: any) {
      setItems([])
      setError(e?.message || 'Unable to load events.')
    }
  }

  return (
    <div>
      <header className="mb-4 flex items-center justify-between">
        <h1 className="text-2xl font-semibold text-slate-900">Events</h1>
        <Button onClick={refresh}>Refresh</Button>
      </header>

      {error && (
        <Alert variant="destructive">
          <AlertTitle>Cannot load events</AlertTitle>
          <AlertDescription>{error}</AlertDescription>
        </Alert>
      )}

      <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {items.map((item) => (
          <Card key={item.id}>
            <CardHeader>
              <CardTitle>{item.title}</CardTitle>
              <CardDescription>{item.date}</CardDescription>
            </CardHeader>
            <CardContent className="grid gap-2">
              <div className="text-sm text-slate-700">
                <Label>Description</Label>
                <p>{item.description}</p>
              </div>
              {item.time && (
                <div className="text-sm text-slate-700">
                  <Label>Time</Label>
                  <p>{item.time}</p>
                </div>
              )}
            </CardContent>
            <CardFooter>
              <Button>Manage</Button>
            </CardFooter>
          </Card>
        ))}
      </div>
    </div>
  )
}

// components/admin/team-manager.tsx
'use client'

import { useState, useEffect } from 'react'
import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Textarea } from '@/components/ui/textarea'

interface TeamMember {
  id: string
  name: string
  title: string
  bio: string
  headshot_url?: string
}

export default function TeamManager() {
  const [items, setItems] = useState<TeamMember[]>([])
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    refresh()
  }, [])

  async function refresh() {
    setError(null)
    try {
      const res = await fetch('/api/admin/team', { cache: 'no-store' })
      let data: any = null
      try {
        data = await res.json()
      } catch {
        const text = await res.text().catch(() => '')
        if (!res.ok) {
          setItems([])
          setError(text || `Request failed (${res.status})`)
          return
        }
        data = { items: [] }
      }
      if (!res.ok) {
        const msg =
          data?.error ||
          (res.status === 401 ? 'Please sign in as an admin to manage team members.' : `Request failed (${res.status})`)
        setItems([])
        setError(msg)
        return
      }
      setItems(Array.isArray(data.items) ? data.items : [])
    } catch (e: any) {
      setItems([])
      setError(e?.message || 'Unable to load team.')
    }
  }

  return (
    <div>
      <header className="mb-4 flex items-center justify-between">
        <h1 className="text-2xl font-semibold text-slate-900">Team</h1>
        <Button onClick={refresh}>Refresh</Button>
      </header>

      {error && (
        <Alert variant="destructive">
          <AlertTitle>Cannot load team</AlertTitle>
          <AlertDescription>{error}</AlertDescription>
        </Alert>
      )}

      <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {items.map((item) => (
          <Card key={item.id}>
            <CardHeader>
              <CardTitle>{item.name}</CardTitle>
              <CardDescription>{item.title}</CardDescription>
            </CardHeader>
            <CardContent className="grid gap-2">
              <div className="text-sm text-slate-700">
                <Label>Bio</Label>
                <p>{item.bio}</p>
              </div>
              {item.headshot_url && (
                <div className="text-sm text-slate-700">
                  <Label>Headshot URL</Label>
                  <p>{item.headshot_url}</p>
                </div>
              )}
            </CardContent>
            <CardFooter>
              <Button>Manage</Button>
            </CardFooter>
          </Card>
        ))}
      </div>
    </div>
  )
}

// components/admin/store-manager.tsx
'use client'

import { useState, useEffect } from 'react'
import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Textarea } from '@/components/ui/textarea'

interface StoreItem {
  id: string
  title: string
  description: string
  category: string
  image_url?: string
  external_url: string
}

export default function StoreManager() {
  const [items, setItems] = useState<StoreItem[]>([])
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    refresh()
  }, [])

  async function refresh() {
    setError(null)
    try {
      const res = await fetch('/api/admin/store', { cache: 'no-store' })
      let data: any = null
      try {
        data = await res.json()
      } catch {
        const text = await res.text().catch(() => '')
        if (!res.ok) {
          setItems([])
          setError(text || `Request failed (${res.status})`)
          return
        }
        data = { items: [] }
      }
      if (!res.ok) {
        const msg =
          data?.error ||
          (res.status === 401 ? 'Please sign in as an admin to manage store items.' : `Request failed (${res.status})`)
        setItems([])
        setError(msg)
        return
      }
      setItems(Array.isArray(data.items) ? data.items : [])
    } catch (e: any) {
      setItems([])
      setError(e?.message || 'Unable to load store items.')
    }
  }

  return (
    <div>
      <header className="mb-4 flex items-center justify-between">
        <h1 className="text-2xl font-semibold text-slate-900">Store</h1>
        <Button onClick={refresh}>Refresh</Button>
      </header>

      {error && (
        <Alert variant="destructive">
          <AlertTitle>Cannot load store</AlertTitle>
          <AlertDescription>{error}</AlertDescription>
        </Alert>
      )}

      <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {items.map((item) => (
          <Card key={item.id}>
            <CardHeader>
              <CardTitle>{item.title}</CardTitle>
              <CardDescription>{item.category}</CardDescription>
            </CardHeader>
            <CardContent className="grid gap-2">
              <div className="text-sm text-slate-700">
                <Label>Description</Label>
                <p>{item.description}</p>
              </div>
              <div className="text-sm text-slate-700">
                <Label>External URL</Label>
                <p>{item.external_url}</p>
              </div>
              {item.image_url && (
                <div className="text-sm text-slate-700">
                  <Label>Image URL</Label>
                  <p>{item.image_url}</p>
                </div>
              )}
            </CardContent>
            <CardFooter>
              <Button>Manage</Button>
            </CardFooter>
          </Card>
        ))}
      </div>
    </div>
  )
}
